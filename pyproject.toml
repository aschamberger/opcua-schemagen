[project]
name = "python-opc-ua-models"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "asyncua>=1.1.8",
    "datamodel-code-generator>=0.50.0",
    "typer>=0.20.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
